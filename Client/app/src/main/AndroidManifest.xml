<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.client">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".c3Notes"
            android:label="Client 3 Notes" />
        <activity
            android:name=".c2Notes"
            android:label="Client 2 Notes" />
        <activity
            android:name=".c1Notes"
            android:label="Client 1 Notes" />
        <activity
            android:name=".cTimer"
            android:label="Client 3 Timer" />
        <activity
            android:name=".bTimer"
            android:label="Client 2 Timer" />
        <activity
            android:name=".aTimer"
            android:label="Client 1 Timer" />
        <activity android:name=".Trial1Timer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>